{
  "version": 2,
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }],
  "old-settings": {
    "builds": [
      {
        "src": "server.js",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "server.js"
      }
    ]
  }
}